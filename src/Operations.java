public interface Operations <A>{
    String addition(A number1, A number2);
    String subtract(A number1, A number2);
    String multiplication(A number1, A number2);
    String division(A number1, A number2);

}
