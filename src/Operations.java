public interface Operations <A, B>{
    double sum(A number1, B number2);
    double subtract(A number1, B number2);
    double multiplication(A number1, B number2);
    double division(A number1, B number2);
}
